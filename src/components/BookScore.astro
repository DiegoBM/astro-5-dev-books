---
import { SCORE_API_ENDPOINT } from "astro:env/server";

interface Props {
  id: string;
}

const { id } = Astro.props;
const scoreValue = await fetch(SCORE_API_ENDPOINT).then((res) => res.text());
---

<span class="text-xs" id="score">⭐ Puntuación: {scoreValue.trim()}/5 </span>
